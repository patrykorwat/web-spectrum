# SDRPlay Bridge Requirements
# Install with: pip install -r requirements.txt

# NOTE: SoapySDR Python bindings are installed via Homebrew (brew install soapysdr)
# They are NOT available via pip

# WebSocket server
websockets>=12.0

# Numpy for sample processing
numpy>=1.24.0
